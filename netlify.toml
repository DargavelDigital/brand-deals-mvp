[build]
  command = "pnpm run build:netlify"
  publish = ".next"

[functions]
  node_bundler = "esbuild"

# Heavy function override (PDF)
[[functions]]
  path = "/api/media-pack/generate"
  memory = 1024
  timeout = 120
