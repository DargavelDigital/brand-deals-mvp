[build]
  command = "pnpm install && pnpm prisma migrate deploy && pnpm build"
  publish = ".next"

[functions]
  node_bundler = "esbuild"

[functions."/api/media-pack/generate"]
  timeout = 120
  memory = 1024
