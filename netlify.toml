[build]
  command = "pnpm prisma migrate deploy && pnpm build"
  publish = ".next"

[functions]
  node_bundler = "esbuild"

[functions."/api/media-pack/generate"]
  memory = 1024
  timeout = 120

[functions."/api/media-pack/file/*"]
  memory = 512
  timeout = 30
